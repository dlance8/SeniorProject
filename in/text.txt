rootNode

translate(rootNode);

translate(Node node){
    switch (node.value) {
        // ...
        case LOCAL_VARIABLE_DECLARATION:
            // translate local variable declaration

            Node type = node.getChildren().get(0);
            // skip this

            Node name = node.getChildren().get(1);

            if (!isLegalPythonName(name.text()))
                // do something

            print(name.text());

            Node eql  = node.getChildren().get(2);

            print(eql);

            Node val  = node.getChildren().get(3);
    }

}
LocalVariableDeclaration = ( "int" | "double" | "float" ) , identifier , "=" , NumberLiteral ;


x = 12 ;